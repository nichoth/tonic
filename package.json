{
  "name": "@bicycle-codes/tonic",
  "version": "15.3.4",
  "description": "A component framework.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "lint": "eslint \"./src/index.js\"",
    "test": "npm run build && npm run lint && esbuild --bundle test/index.js | tape-run",
    "ci:test:tape-run": "esbuild --bundle test/index.js | tape-run",
    "test:open": "npm run build && esbuild --bundle test/index.js | tape-run --browser chrome --keep-open",
    "build:base": "esbuild src/index.js --define:VERSION=\\\"$npm_package_version\\\" --outfile=dist/index.js",
    "build:minify": "esbuild src/index.js --keep-names --minify --outfile=dist/tonic.min.js",
    "build": "mkdir -p ./dist && rm -rf ./dist/* && npm run build:base && npm run build:minify",
    "preversion": "npm run lint",
    "version": "auto-changelog -p --template keepachangelog auto-changelog --breaking-pattern 'BREAKING CHANGE:' && git add CHANGELOG.md",
    "postversion": "git push --follow-tags && npm publish",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "@bicycle-codes/tapzero": "0.9.2",
    "auto-changelog": "2.4.0",
    "benchmark": "^2.1.4",
    "esbuild": "^0.20.2",
    "eslint": "^8.57.0",
    "eslint-config-standard": "^17.1.0",
    "tape-run": "^11.0.0",
    "uuid": "^9.0.0"
  },
  "exports": {
    ".": "./index.js",
    "./*.min.js": "./dist/*.min.js"
  },
  "contributors": [
    {
      "name": "nichoth",
      "email": "nichoth@nichoth.com",
      "url": "https://nichoth.com/"
    },
    {
      "name": "Raynos",
      "email": "raynos2@gmail.com"
    }
  ],
  "license": "MIT",
  "standard": {
    "ignore": [
      "test/fixtures/*"
    ]
  },
  "directories": {
    "test": "test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nichoth/tonic.git"
  },
  "bugs": {
    "url": "https://github.com/nichoth/tonic/issues"
  },
  "homepage": "https://tonicframework.dev"
}
